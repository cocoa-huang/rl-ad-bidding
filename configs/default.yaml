environment:
  budget: 1000
  n_competitors: 47
  episode_length: 1000

agent:
  algorithm: ppo
  lr: 3e-4
  gamma: 0.99
  batch_size: 64

training:
  n_episodes: 1000
  eval_interval: 100
  save_interval: 500

logging:
  use_wandb: true
  project_name: rl-ad-bidding
  run_name: baseline
